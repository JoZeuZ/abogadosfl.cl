---
// Lawyer Profile Component
const lawyer = {
  name: 'Dr. Patolo Francisco',
  title: 'Abogado Especialista',
  specialties: ['Derecho Civil', 'Derecho Penal', 'Derecho Laboral'],
  image: '/images/abogado_horizontal1.png',
  experience: '5 años',
  education: [
    'Universidad del Desarrollo - Licenciatura en Derecho',
    'Magíster en Derecho Penal'
  ],
  awards: [
    'Distinción Máxima en Litigación',
    'Premio Excelencia Académica 2020'
  ],
  description: `Especializado en casos complejos de derecho civil y penal, con un historial 
    comprobado de éxito en la defensa de los derechos de mis clientes. Mi enfoque se 
    centra en proporcionar soluciones legales efectivas y personalizadas.`,
  social: {
    linkedin: '#',
    email: 'francisco@abogadosfl.cl'
  }
};
---

<section id="abogado" class="lawyer section-padding">
  <div class="container">
    <div class="section-header text-center fade-in">
      <span class="section-subtitle">Perfil Profesional</span>
      <h2 class="section-title">
        Experiencia y <span class="text-accent">Compromiso</span>
      </h2>
      <p class="section-description">
        Dedicado a brindar asesoría legal de excelencia, combinando experiencia práctica 
        con un profundo conocimiento del derecho y un compromiso inquebrantable con mis clientes.
      </p>
    </div>
    
    <div class="lawyer-profile fade-in">
      <div class="lawyer-image">
        <img src={lawyer.image} alt={lawyer.name} />
        <div class="lawyer-social">
          <a href={lawyer.social.linkedin} class="social-link" aria-label="LinkedIn">
            <i class="fab fa-linkedin-in"></i>
          </a>
          <a href={`mailto:${lawyer.social.email}`} class="social-link" aria-label="Email">
            <i class="fas fa-envelope"></i>
          </a>
        </div>
      </div>
      
      <div class="lawyer-content">
        <div class="lawyer-header">
          <h3 class="lawyer-name">{lawyer.name}</h3>
          <p class="lawyer-title">{lawyer.title}</p>
        </div>

        <p class="lawyer-bio">{lawyer.description}</p>
        
        <div class="lawyer-specialties">
          <h4>Áreas de Práctica</h4>
          <div class="specialty-tags">
            {lawyer.specialties.map((specialty) => (
              <span class="specialty-tag">{specialty}</span>
            ))}
          </div>
        </div>
        
        <div class="lawyer-details">
          <div class="detail-group">
            <h4>Educación</h4>
            <ul>
              {lawyer.education.map((edu) => (
                <li>{edu}</li>
              ))}
            </ul>
          </div>
          
          <div class="detail-group">
            <h4>Reconocimientos</h4>
            <ul>
              {lawyer.awards.map((award) => (
                <li>{award}</li>
              ))}
            </ul>
          </div>
        </div>
      </div>
    </div>
    
    <div class="lawyer-cta text-center fade-in">
      <h3>¿Necesita asesoría legal especializada?</h3>
      <p>Estoy aquí para ayudarle con su caso</p>
      <a href="#contacto" class="btn btn-primary btn-lg">Agendar Consulta</a>
    </div>
  </div>
</section>

<style>
  .lawyer {
    @apply bg-light py-20;
  }
  
  .section-header {
    @apply max-w-2xl mx-auto mb-16 text-center;
  }
  
  .section-subtitle {
    @apply text-accent font-semibold uppercase tracking-wider text-sm;
  }
  
  .lawyer-profile {
    @apply grid grid-cols-1 lg:grid-cols-2 gap-12 max-w-6xl mx-auto mb-20;
  }
  
  .lawyer-image {
    @apply relative;
  }
  
  .lawyer-image img {
    @apply w-full h-auto rounded-lg shadow-xl;
  }
  
  .lawyer-social {
    @apply absolute bottom-4 right-4 flex gap-3;
  }
  
  .social-link {
    @apply w-10 h-10 bg-accent text-white rounded-full flex items-center justify-center
           transition-transform hover:scale-110;
  }
  
  .lawyer-content {
    @apply space-y-8;
  }
  
  .lawyer-header {
    @apply mb-6;
  }
  
  .lawyer-name {
    @apply text-3xl font-bold text-primary mb-2;
  }
  
  .lawyer-title {
    @apply text-xl text-accent font-medium;
  }
  
  .lawyer-bio {
    @apply text-lg leading-relaxed text-gray-700;
  }
  
  .lawyer-specialties {
    @apply space-y-4;
  }
  
  .specialty-tags {
    @apply flex flex-wrap gap-3;
  }
  
  .specialty-tag {
    @apply bg-primary/10 text-primary px-4 py-2 rounded-full font-medium;
  }
  
  .lawyer-details {
    @apply grid grid-cols-1 md:grid-cols-2 gap-8;
  }
  
  .detail-group {
    @apply space-y-3;
  }
  
  .detail-group h4 {
    @apply text-lg font-semibold text-primary mb-2;
  }
  
  .detail-group ul {
    @apply space-y-2 text-gray-700;
  }
  
  .lawyer-cta {
    @apply max-w-2xl mx-auto text-center;
  }
  
  .lawyer-cta h3 {
    @apply text-2xl font-bold mb-3;
  }
  
  .lawyer-cta p {
    @apply text-lg text-gray-600 mb-6;
  }
  
  .section-title {
    @apply text-4xl font-bold mb-4;
    color: var(--color-primary);
    margin: 0.5rem 0 1rem;
  }
  
  .section-description {
    font-size: 1.1rem;
    color: var(--color-text-light);
    line-height: 1.6;
  }
  
  .team-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: 2rem;
    margin-bottom: 4rem;
  }
  
  .team-card {
    background-color: var(--color-white);
    border-radius: 15px;
    overflow: hidden;
    box-shadow: var(--shadow-card);
    transition: all 0.3s ease;
  }
  
  .team-card:hover {
    transform: translateY(-10px);
    box-shadow: var(--shadow-hover);
  }
  
  .team-image {
    position: relative;
    overflow: hidden;
    height: 280px;
  }
  
  .team-image img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    transition: transform 0.3s ease;
  }
  
  .team-card:hover .team-image img {
    transform: scale(1.1);
  }
  
  .team-overlay {
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: linear-gradient(to bottom, transparent 0%, rgba(31, 44, 61, 0.8) 100%);
    display: flex;
    align-items: flex-end;
    justify-content: center;
    padding: 1.5rem;
    opacity: 0;
    transition: opacity 0.3s ease;
  }
  
  .team-card:hover .team-overlay {
    opacity: 1;
  }
  
  .team-social {
    display: flex;
    gap: 1rem;
  }
  
  .social-link {
    width: 40px;
    height: 40px;
    border-radius: 50%;
    background-color: var(--color-accent);
    color: var(--color-white);
    display: flex;
    align-items: center;
    justify-content: center;
    text-decoration: none;
    transition: all 0.3s ease;
  }
  
  .social-link:hover {
    background-color: var(--color-white);
    color: var(--color-accent);
    transform: translateY(-3px);
  }
  
  .team-content {
    padding: 2rem;
    text-align: center;
  }
  
  .team-name {
    font-size: 1.4rem;
    font-weight: 600;
    color: var(--color-primary);
    margin-bottom: 0.5rem;
    font-family: var(--font-heading);
  }
  
  .team-position {
    color: var(--color-accent);
    font-weight: 600;
    margin-bottom: 0.5rem;
  }
  
  .team-specialty {
    color: var(--color-text-light);
    font-size: 0.95rem;
    margin-bottom: 1.5rem;
  }
  
  .team-details {
    margin-bottom: 1.5rem;
    text-align: left;
  }
  
  .team-detail {
    display: flex;
    justify-content: space-between;
    margin-bottom: 0.5rem;
    font-size: 0.9rem;
  }
  
  .detail-label {
    color: var(--color-text-light);
    font-weight: 500;
  }
  
  .detail-value {
    color: var(--color-text);
    font-weight: 600;
  }
  
  .team-contact {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    color: var(--color-accent);
    text-decoration: none;
    font-weight: 600;
    transition: all 0.3s ease;
    padding: 0.5rem 1rem;
    border-radius: 25px;
    border: 2px solid var(--color-accent);
  }
  
  .team-contact:hover {
    background-color: var(--color-accent);
    color: var(--color-white);
    transform: translateX(5px);
  }
  
  .team-cta {
    background-color: var(--color-white);
    padding: 3rem 2rem;
    border-radius: 15px;
    box-shadow: var(--shadow-card);
  }
  
  .team-cta h3 {
    color: var(--color-primary);
    font-size: 1.8rem;
    margin-bottom: 1rem;
  }
  
  .team-cta p {
    color: var(--color-text-light);
    font-size: 1.1rem;
    margin-bottom: 2rem;
  }
  
  @media (max-width: 768px) {
    .team-grid {
      grid-template-columns: 1fr;
    }
    
    .section-title {
      font-size: 2rem;
    }
    
    .team-image {
      height: 250px;
    }
    
    .team-content {
      padding: 1.5rem;
    }
    
    .team-cta {
      padding: 2rem 1.5rem;
    }
    
    .team-cta h3 {
      font-size: 1.5rem;
    }
  }
</style>
