<section id="inicio" class="hero">
  <div class="hero-container">
    <div class="hero-content">
      <div class="hero-text fade-in">
        <h1 class="hero-title">
          Defiendo<br/>
          sus Derechos con<br/>
          <span class="text-accent">Excelencia y<br/>Dedicación</span>
        </h1>
        <p class="hero-subtitle">
          Brindo asesoría legal de primera calidad. 
          Mi compromiso es proteger sus intereses con la máxima profesionalidad.
        </p>
        <div class="hero-actions">
          <a href="#contacto" class="btn btn-primary btn-lg">Consulta</a>
          <a href="#sobre-nosotros" class="btn btn-outline btn-lg">Conoce Más</a>
        </div>
      </div>
      
      <div class="hero-image-wrapper fade-in">
        <img src="/images/abogado2.png" alt="Abogado profesional de Abogados FL posando con confianza en un entorno de oficina moderno." class="hero-image">
        
        <div class="hero-stats" id="hero-stats">
          <div class="stat-item">
            <div class="stat-number" data-target="100">0</div>
            <div class="stat-label">Casos Exitosos</div>
          </div>
          <div class="stat-item">
            <div class="stat-number" data-target="5">0</div>
            <div class="stat-label">Años de Experiencia</div>
          </div>
          <div class="stat-item">
            <div class="stat-number" data-target="98">0</div>
            <div class="stat-label">% Casos Ganados</div>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  /* ==========================================================================
     Estilos de la Sección Hero
     ========================================================================== */
  .hero {
    background-color: var(--color-primary);
    color: var(--color-white);
    padding-top: 6rem; /* Más espacio superior */
    padding-bottom: 6rem; /* Más espacio inferior */
    position: relative;
    overflow: hidden;
    display: flex;
    align-items: center;
    overflow: hidden;
    position: relative;
  }

  .hero::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background-image: url('/images/pattern.png');
    background-size: cover;
    opacity: 0.05;
    pointer-events: none;
  }

  .hero-content {
    position: relative;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 3rem;
    text-align: center;
  }

  .hero-text {
    max-width: 650px;
    margin: 0 auto;
  }
  
  .hero-title {
    font-size: 3rem;
    line-height: 1.1;
    margin-bottom: 1.5rem;
    font-weight: 600;
    color: var(--color-white);
  }
  
  .hero-subtitle {
    font-size: 1.125rem;
    color: var(--color-text-light);
    margin-bottom: 2.5rem;
    line-height: 1.8;
  }
  
  .hero-actions {
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    gap: 1.5rem;
  }
  
  .hero-image-wrapper {
    position: relative;
    margin: 0 auto;
    width: 100%;
    max-width: 480px;
  }
  
  .hero-image {
    width: 100%;
    height: auto;
    object-fit: cover;
    position: relative;
    z-index: 1;
  }

  .hero-image-wrapper::before {
    content: '';
    position: absolute;
    top: -2rem;
    right: -2rem;
    width: 100%;
    height: 100%;
    border: 2px solid var(--color-accent);
    z-index: 0;
  }

  .hero-stats {
    position: absolute;
    bottom: -2rem;
    left: 50%;
    transform: translateX(-50%);
    background-color: var(--color-white);
    padding: 2rem;
    box-shadow: var(--shadow-card);
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 2rem;
    width: calc(100% - 4rem);
    max-width: 600px;
    z-index: 2;
  }

  .stat-item {
    text-align: center;
    position: relative;
  }

  .stat-item:not(:last-child)::after {
    content: '';
    position: absolute;
    right: -1rem;
    top: 50%;
    transform: translateY(-50%);
    height: 40px;
    width: 1px;
    background-color: var(--color-border);
  }

  .stat-number {
    font-size: 2.5rem;
    font-weight: 700;
    font-family: var(--font-heading);
    color: var(--color-primary);
    line-height: 1;
    margin-bottom: 0.5rem;
  }

  .stat-label {
    font-size: 0.875rem;
    color: var(--color-text-muted);
    text-transform: uppercase;
    letter-spacing: 1px;
  }

  /* Media Query para Tablet y superior */
  @media (min-width: 768px) {
    .hero {
      padding-top: 5rem; /* Altura del header desktop */
      padding-bottom: 8rem; /* Espacio para las estadísticas */
    }

    .hero-title {
      font-size: 3.5rem;
    }

    .hero-subtitle {
      font-size: 1.25rem;
      max-width: 80%;
      margin-left: auto;
      margin-right: auto;
    }

    .hero-stats {
      padding: 2.5rem 3rem;
    }

    .stat-number {
      font-size: 3rem;
    }

    .stat-label {
      font-size: 0.9375rem;
    }
  }

  /* Media Query para Desktop */
  @media (min-width: 1024px) {
    .hero {
      min-height: 90vh;
      padding-bottom: 6rem;
    }

    .hero-content {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 4rem;
      align-items: center;
      text-align: left;
    }

    .hero-text {
      margin: 0;
    }

    .hero-title {
      font-size: 4rem;
    }

    .hero-subtitle {
      margin-left: 0;
      margin-right: 0;
      max-width: 90%;
    }

    .hero-actions {
      justify-content: flex-start;
    }

    .hero-image-wrapper {
      margin-right: -4rem;
    }

    .hero-stats {
      left: 0;
      transform: none;
      padding: 3rem;
    }
  }

  /* Media Query para pantallas grandes */
  @media (min-width: 1280px) {
    .hero-title {
      font-size: 4.5rem;
    }

    .hero-subtitle {
      font-size: 1.375rem;
    }

    .hero-image-wrapper {
      margin-right: -6rem;
    }

    .hero-stats {
      padding: 3.5rem;
      gap: 3rem;
    }

    .stat-number {
      font-size: 3.5rem;
    }
  }
</style>
