<section class="stats-bar dark-overlay" id="stats-bar">
  <div class="container">
    <div class="stats-grid">
      <div class="stat-card fade-in">
        <div class="stat-icon">
          <i class="fas fa-gavel"></i>
        </div>
        <div class="stat-content">
          <div class="stat-number" data-target="100">0</div>
          <div class="stat-label">Casos Resueltos</div>
        </div>
      </div>
      
      <div class="stat-card fade-in">
        <div class="stat-icon">
          <i class="fas fa-trophy"></i>
        </div>
        <div class="stat-content">
          <div class="stat-number" data-target="98" data-suffix="%">0</div>
          <div class="stat-label">Éxito</div>
        </div>
      </div>
      
      <div class="stat-card fade-in">
        <div class="stat-icon">
          <i class="fas fa-users"></i>
        </div>
        <div class="stat-content">
          <div class="stat-number" data-target="90" data-suffix="+">0</div>
          <div class="stat-label">Clientes Satisfechos</div>
        </div>
      </div>
      
      <div class="stat-card fade-in">
        <div class="stat-icon">
          <i class="fas fa-award"></i>
        </div>
        <div class="stat-content">
          <div class="stat-number" data-target="5" data-suffix="+">0</div>
          <div class="stat-label">Años Experiencia</div>
        </div>
      </div>
    </div>
  </div>
</section>


<script>
import { initNumberAnimation } from "../utils/animations";
  document.addEventListener('DOMContentLoaded', () => {
    const statsSection = document.getElementById('stats-bar');
    if (statsSection) {
      initNumberAnimation(statsSection);
    }
  });
</script>

<style>
  /* ==========================================================================
     Estilos de la Barra de Estadísticas
     ========================================================================== */
  .stats-bar {
    padding: 6rem 0;
    position: relative;
    overflow: hidden;
    background-color: var(--color-primary);
    color: var(--color-white);
  }
  
  .stats-bar::before {
    content: '';
    position: absolute;
    inset: 0;
    background: linear-gradient(rgba(31, 44, 61, 0.95), rgba(31, 44, 61, 0.95)), 
                url('/images/pasillo_oficina.png') center/cover no-repeat;
    z-index: 0;
  }
  
  .stats-bar .container {
    position: relative;
    z-index: 1;
  }
  
  .stats-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 3rem;
  }

  @media (min-width: 768px) {
    .stats-grid {
      grid-template-columns: repeat(4, 1fr);
    }
  }

  .stat-card {
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
    gap: 1.5rem;
    padding: 1.5rem;
    background: rgba(255, 255, 255, 0.03);
    border-radius: var(--radius-md);
    backdrop-filter: blur(10px);
    transition: transform 0.3s ease;
  }

  .stat-card:hover {
    transform: translateY(-5px);
  }

  .stat-icon {
    color: var(--color-accent);
    font-size: 2.5rem;
    margin-bottom: 0.5rem;
  }

  .stat-number {
    font-size: 2.25rem;
    font-weight: 700;
    color: var(--color-white);
    font-family: var(--font-heading);
    line-height: 1;
  }

  .stat-label {
    font-size: 0.9rem;
    color: var(--color-light);
    text-transform: uppercase;
    letter-spacing: 1px;
    opacity: 0.8;
  }

  /* Media Query para Tablet y superior */
  @media (min-width: 768px) {
    .stats-bar {
      padding: 4rem 0;
    }
    .stats-grid {
      grid-template-columns: repeat(4, 1fr);
    }
    .stat-icon {
      font-size: 2.5rem;
    }
  }
</style>
