

<section class="stats-bar" id="stats-bar">
  <div class="container">
    <div class="stats-grid">
      <div class="stat-card fade-in">
        <div class="stat-icon">
          <i class="fas fa-gavel"></i>
        </div>
        <div class="stat-content">
          <div class="stat-number" data-target="100">0</div>
          <div class="stat-label">Casos Resueltos</div>
        </div>
      </div>
      
      <div class="stat-card fade-in">
        <div class="stat-icon">
          <i class="fas fa-trophy"></i>
        </div>
        <div class="stat-content">
          <div class="stat-number" data-target="98" data-suffix="%">0</div>
          <div class="stat-label">Éxito</div>
        </div>
      </div>
      
      <div class="stat-card fade-in">
        <div class="stat-icon">
          <i class="fas fa-users"></i>
        </div>
        <div class="stat-content">
          <div class="stat-number" data-target="90" data-suffix="+">0</div>
          <div class="stat-label">Clientes Satisfechos</div>
        </div>
      </div>
      
      <div class="stat-card fade-in">
        <div class="stat-icon">
          <i class="fas fa-award"></i>
        </div>
        <div class="stat-content">
          <div class="stat-number" data-target="5" data-suffix="+">0</div>
          <div class="stat-label">Años Experiencia</div>
        </div>
      </div>
    </div>
  </div>
</section>


<script>
import { initNumberAnimation } from "../utils/animations";
  document.addEventListener('DOMContentLoaded', () => {
    const statsSection = document.getElementById('stats-bar');
    if (statsSection) {
      initNumberAnimation(statsSection);
    }
  });
</script>

<style>
  .stats-bar {
    padding: 60px 0;
    position: relative;
    overflow: hidden;
    background-color: rgba(31, 44, 61, 0.9); /* Color primario con transparencia */
  }
  
  .stats-bar::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: url('/images/pasillo_oficina.png') center/cover no-repeat;
    opacity: 0.15;
    z-index: 0;
  }
  
  .container {
    position: relative;
    z-index: 1;
  }
  
  .stats-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 2rem;
  }
  
  .stat-card {
    @apply text-center text-white;
    padding: 1rem;
    transition: transform 0.3s ease;
  }
  
  .stat-card:hover {
    transform: translateY(-10px);
  }
  
  .stat-icon {
    @apply bg-accent text-white w-20 h-20 rounded-full flex items-center justify-center mx-auto mb-6 shadow-card;
  }
  
  .stat-icon i {
    @apply text-3xl;
  }
  
  .stat-number {
    @apply text-4xl font-bold font-heading text-accent mb-2;
  }
  
  .stat-label {
    @apply text-base font-medium opacity-90 uppercase tracking-wider;
  }
  
  @media (max-width: 992px) {
    .stats-grid {
      grid-template-columns: repeat(2, 1fr);
      gap: 3rem;
    }
  }
  
  @media (max-width: 576px) {
    .stats-grid {
      grid-template-columns: 1fr;
      gap: 2rem;
    }
    
    .stat-number {
      font-size: 2.5rem;
    }
    
    .stat-icon {
      width: 70px;
      height: 70px;
    }
    
    .stat-icon i {
      font-size: 1.5rem;
    }
  }
</style>
